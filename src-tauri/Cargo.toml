[package]
name = "frontier"
version = "0.1.0"
description = "Local-first AI and scientific workbench"
authors = ["Shoko-official"]
edition = "2021"

[features]
managed-engine = []

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
