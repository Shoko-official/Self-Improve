# Automations

Automation definitions and run history are durable. Runs default to dry-run and record a simulated terminal state. Definitions with external effects cannot execute unless explicitly approved; dry runs do not bypass that boundary.
