# Notifications

Notifications retain their source, severity, concise body, deep link, and acknowledgement state. Bodies are rejected when they contain obvious credential markers, so lock-screen delivery can use the stored concise summary without source text or secrets.
