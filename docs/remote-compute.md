# Remote compute

Every plan contains an exact command, target, CPU, memory, timeout, cost estimate, and data-egress estimate. A remote SSH, SLURM, or cloud plan is rejected until explicitly approved. Local execution records terminal state, stdout, stderr, and timeout diagnostics.

This slice includes a tested local fixture only. It does not claim a live SSH, scheduler, or cloud provider submission.
