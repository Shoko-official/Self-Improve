# Prompt system

Frontier uses original compact, standard, and extended prompt variants. Each has a token budget checked by the compiler before use. Prompt content is generated from Frontier's own local-first, permission, provenance, and scientific-integrity rules; supplied provider prompt material is not copied.

Project instructions and granted tool names are explicit compiler inputs. An oversized instruction set is rejected rather than silently truncated.
