# Benchmark methodology

Each report records raw cold/warm samples, latency, throughput, peak memory, variant identity, and environment fingerprint. Summaries are arithmetic aggregates over stored samples. Comparison exports show deltas only; they do not generalize beyond their recorded environment and inputs.
